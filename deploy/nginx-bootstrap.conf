# Temporary HTTP configuration to obtain the first certificate.
# Remove this config when installing nginx.conf: never enable both together.
server {
    listen 80;
    server_name www.sansphase.com;
    location ^~ /.well-known/acme-challenge/ { root /var/www/letsencrypt; }
    location / { return 503; }
}
