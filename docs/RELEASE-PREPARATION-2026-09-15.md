# 发布候选版整理记录 · 2026-09-15

状态：源码已发布至公开仓库，腾讯云正式部署与基础生产验收已完成。以下保留各阶段记录，最新生产状态见文末追加验收。

仓库：[m17639340459gmailcom/sansphase-site](https://github.com/m17639340459gmailcom/sansphase-site)。默认分支为 `codex/release-preparation`，当前仍为发布候选版。

## 已落实

- 包名称 sansphase-site，版本 1.0.0-rc.1；目标域名 www.sansphase.com，仓库选择公开。
- 删除访客页面模拟登录、发帖、回复、签到和联系草稿提交逻辑。社区等未开放功能给出真实状态，不伪装提交成功。
- 前台不再回退到写死的样例文章、资料与公告；测试数据留在 tests/fixtures。
- 61 个历史源码、旧测试及一次性脚本移到本地 archive/release-cleanup-20260915，当前构建与测试不依赖这个目录。真实文章、数据库、上传和首页特效没有被删除或改换。
- 作者服务必须显式使用已配置的存储，正式进程不再导入旧 Directus 作者适配器。兼容已迁移内容的字段和 URL 重写保留。
- 正式启动入口校验 HTTPS 站点地址、私有配置位置、构建文件 SHA-256；/healthz 返回运行版本及数据库可读状态。
- 静态资源 ETag 与内容摘要 chunk 缓存；作者身份相关 HTML/API 不缓存；日志不记录 Cookie、请求体与查询参数。
- 提供 Ubuntu systemd、Nginx HTTPS/登录限流/15 GiB 上传和备份 timer 模板，以及域名、备份、恢复、回滚说明。
- 公开源码采用明确文件清单并扫描已知私有值，保留许可证。项目所有者已明确确认首页参考素材的完整使用权，授权说明随来源记录保留。
- GitHub CI 已在 Ubuntu 24.04 实际执行：安装依赖、构建与 149 项测试全部通过。自动流程保持只读仓库权限；随后将 Actions 更新到 Node 24 运行时并固定提交版本，避免旧运行时弃用警告。

## 验证

当前源码构建通过；149 项当前功能与发布测试全部通过，0 失败、0 跳过。包含首次/刷新布局、作者操作、草稿发布、媒体授权、安装包上传下载、缓存、构建完整性，以及生产入口连接隔离数据库后的真实 HTTP 检查。历史旧场景测试已随旧实现归档，因此不与之前的 284 项按数量直接比较。

本地报告：outputs/verification/release-final-build.log、release-final-tests.log、release-production-runtime.log。之前 15 GiB 实际传输与哈希校验记录保留，本轮未重复传输 15 GiB。

追加验证：已将 209 个允许公开的文件保存为独立的本地 Git 提交，再从该提交克隆到开发目录之外的全新目录。使用锁文件重新安装依赖、构建并执行 149 项测试，全部通过，0 失败、0 跳过；没有借用开发目录的 node_modules。该验证在 Windows 上执行，Ubuntu CI 和服务器验收仍待完成。记录保存在 outputs/verification/release-clean-install.log、release-clean-checkout-build.log、release-clean-checkout-tests.log。

首次远程验证：[GitHub Actions 34959513876](https://github.com/m17639340459gmailcom/sansphase-site/actions/runs/34959513876)，提交 `4a18fe8`，Ubuntu 24.04，149 通过、0 失败、0 跳过。公开文件清单为 210 个文件（增加仓库头图），已核对远程提交与本地提交一致，已知私有值扫描和文档相对链接检查通过。当前提交的检查状态以仓库 Actions 为准。

## 生产部署追加验收（2026-09-15）

应用提交 `dc44044` 已部署至腾讯云 Ubuntu 24.04，正式入口为 https://www.sansphase.com/ 。运行环境为 Node.js 24.21.0、pnpm 11.19.0、Nginx 与 systemd；运行数据独立于版本目录。页脚使用站长提供并确认的备案号“豫ICP备2026037683号-1”，链接至工信部备案查询入口。

- 服务器从提交归档安装锁定依赖、构建，149 项测试通过，0 失败、0 跳过。
- 同一应用提交的 [GitHub Actions 34961690980](https://github.com/m17639340459gmailcom/sansphase-site/actions/runs/34961690980) 已成功完成。证书模拟续期及续期后的 Nginx 配置检查、自动重载均通过。
- 外部 HTTPS 证书验证通过，HTTP 自动跳转；初始 14 项页面资源响应正常，HTML 内嵌内容与公开 API 一致。
- 匿名用户无法读取作者资料、上传接口和私有媒体；私有配置、数据库、源码路径及跨站写入请求被拒绝。
- 服务重启后健康检查恢复；应用仅监听本机地址，Nginx 提供公网入口，开机启动与定时备份已启用。
- 服务器备份在隔离目录恢复成功，16 张表记录数与迁入数据一致，12 份附件哈希一致；5 份公开媒体通过正式域名读取并比对原始哈希。首份服务器备份已额外保存到站长本机。
- 外部 Chrome 控制桥接仍不可用，本轮没有完成真实浏览器逐页视觉验收；已有 DOM、交互与服务端自动化验证不替代该项。

报告保存在站长本机 outputs/verification/ 下：server-final-release.log、production-http-check.json、server-runtime-check.log、server-restore-check.log。公开仓库不包含数据库、备份、服务器密钥或运行凭据。

## 尚未完成

- 持续自动异地备份目标、保留周期、告警接收方式，以及真实浏览器逐页视觉验收。
- 大文件断点续传、集中媒体永久清理、文章可索引路由与邮件找回密码不属于本轮已完成能力。
- 本地 4176 的旧进程仍需重启以加载新服务端代码。此前自动审批拒绝了停止/重启该进程；新生产入口已在独立测试进程验证，不等同于旧预览进程已更新。

基础部署已完成，不表示上述后续能力已完成，也不意味着不存在未知问题。历史本地验证记录保留，生产状态以本节追加验收为准。
