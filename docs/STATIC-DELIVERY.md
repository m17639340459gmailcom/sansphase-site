# 原画质静态资源交付

首页继续使用原始 JPEG、R 模型、粒子数量和原有渲染参数。资源交付与 GPU 帧耗时分别验收：提高带宽不能降低每帧绘制成本，预加载也不能消除首次下载字节数。

## 当前实现

- 构建从实际产物生成 `startup-assets.js`，仅在首页预加载四张场景图片与渲染模块。直接访问内容页不下载首页场景。
- 原始全景优先，其他首页场景仍在进入前准备完成，避免访客立即滚动时出现缺图。博客媒体在首页准备完成之后预取。
- 写作编辑器在访客点击登录时才加载，首次点击完成加载后继续打开原有面板。博客动态背景按路由加载。
- Nginx 直接发送 `/assets/scene/` 下的公开构建产物，支持范围请求、ETag 和长期缓存。此规则不包含作者上传文件、API、数据库或私有配置。
- 不缩放原图、不转码、不降低粒子数量、渲染分辨率、抗锯齿或材质质量。

## 容量验证

2026-09-15，四个同一客户端的并发连接分别下载 3,446,173 字节图片，用时约 26.8～40.0 秒。服务器同期出口约 3.3 Mbps，CPU 多数采样低于 2%。这只是一次线路实测，不能据此认定套餐带宽，也不是四个不同地区访客的容量验收。

四张首页原图合计 21,305,333 字节。理论下载时间下限为 `总字节数 × 8 / 实际可用带宽`，实际还要计入其他资源、协议、网络波动和客户端准备。一个访问者的本地缓存不会被其他新访客共享；每千次完全无缓存访问仅这四张图片约产生 21.3 GB 传输。

## CDN 配置与发布

### 域名归属权验证

使用 EdgeOne 文件验证时，将控制台下载的原始验证文件放在服务器的 `/var/www/sansphase-verification/.well-known/teo-verification/`。该目录由管理员维护，位于应用发布目录之外；验证文件不进入 Git 仓库。

Nginx 为 HTTP 的 `/.well-known/teo-verification/` 路径直接提供文件，避免跳转至 `www` 或进入应用路由。部署后应确认验证地址返回 200、原始文件字节完全一致、不发生重定向，缺失文件返回 404，首页仍正常跳转至 HTTPS。域名验证通过后，还需完成加速域名、证书、缓存规则和 DNS 配置；文件可访问不代表 CDN 已生效。

### 资源交付配置

已选用 EdgeOne 免费版并完成 `static.sansphase.com` 的 CNAME、边缘 HTTPS 和跨域配置。域名独立防护规则 `static-public-assets-only` 仅允许 `/assets/scene/*`、`/assets/site/*` 与证书验证使用的 `/.well-known/*`，其余路径由边缘节点拦截；主站 HTML 和 API 继续直接连接源站。

构建时可选择公开静态资源交付地址：

```sh
SANSPHASE_SCENE_CDN_ORIGIN=https://static.sansphase.com pnpm build
```

未设置时保留同源路径，适用于本地开发、独立部署及回滚。地址必须是无路径、无用户名密码的 HTTPS origin。四张场景原图保留原有内容指纹地址；程序、样式、字体和材质等公开构建产物组成 `/assets/site/<内容版本>/` 快照。HTML 的脚本及样式引用、模块内部导入、预加载及材质路径使用同一版本，更新会生成新目录；原文件不压缩转码。HTML、作者 API 和上传内容不会写入快照。每次生产构建均需显式保留此设置；它不是启动后的运行时开关。

发布时先将 `dist/assets/site/<内容版本>` 安装到 `/var/www/sansphase-static/assets/site/<内容版本>`，核对 `dist/static-delivery.json` 的文件清单与 SHA-256，再切换应用。Nginx 为这些不可变公开文件提供一年缓存、正确的 `.mjs` MIME 类型和固定 CORS。保留旧快照，使已有页面和回滚后的页面仍能获取对应版本。此公共目录独立于 `/var/lib/sansphase` 私有数据目录，不修改数据库及上传目录的权限。

源站在 `/assets/scene/` 和 `/assets/site/` 设置固定的 `Access-Control-Allow-Origin: https://www.sansphase.com` 与同值的 `Timing-Allow-Origin`，允许本站匿名读取公开资源并测量下载时间。该响应不依赖请求 Origin，无需按 Origin 拆分缓存；首页和作者 API 不添加这些头部。已有节点缓存可能保留旧头部，更新后需执行直接删除缓存，再验证实际节点响应，不能只检查源站。

当前使用站长选择的免费套餐。本次配置不启用付费图片处理；升级套餐或新增计费服务需另行确认。

| 项目 | 配置目标 |
| --- | --- |
| 内容 | `/assets/scene/` 原图、`/assets/site/` 公开前端快照、`/.well-known/` 证书验证；其余路径拒绝 |
| 源站 | 当前源站地址；回源 Host 与 TLS SNI 均为 `www.sansphase.com`，验证证书 |
| 客户端协议 | HTTPS，TLS 1.2/1.3，服务支持时开启 HTTP/2 |
| 图像处理 | 禁止自动压缩、缩放、水印、格式转换，保持原始字节 |
| 缓存 | 内容指纹文件缓存一年；不缓存错误响应；保留旧版本以支持回滚 |
| 跨域 | 针对 `https://www.sansphase.com` 设置图片读取 CORS，不携带账户 Cookie |
| 安全边界 | 登录请求、草稿、上传内容、API、HTML、私有文件不进入此加速域名；编辑器的公开前端程序仍按需加载 |
| 节点准备 | 套餐支持时提交 URL 预热；否则主动请求验证当前节点缓存，不能将单线路验证视为所有节点均已预热 |
| 费用 | 核对地区、流量、HTTPS 请求费及源站流量；配置费用/流量告警和可执行的停用方案 |
| 回滚 | 切回上一发布目录，或清空 `SANSPHASE_SCENE_CDN_ORIGIN` 后重新构建发布；原图和源站服务始终保留 |

启用前应逐图校验源站与加速节点 SHA-256 相同；启用后记录至少多个国内线路的冷缓存加载时间、缓存命中及回源量。限额与告警是否能自动停止计费取决于所选产品，不能把告警当作硬预算封顶。

## 验收要求

1. 首页图片先于场景初始化下载，无重复原图请求；直接访问博客无首页资源请求。
2. 第一次点击登录可打开，加载失败可重试，不需要二次点击或刷新。
3. 全部首页场景完成准备后才进入；原图、模型、材质等保护项通过现有校验。
4. 实测拖拽、滚动、返回首页、后台标签恢复；帧间隔记录硬件、窗口尺寸和采样范围，不承诺所有设备绝对零掉帧。
5. 区分单访客首次加载、再次访问缓存及多人共享出口；公开测试不使用持续高并发压测消耗生产流量。

参考：[Nginx 静态传输指令](https://nginx.org/en/docs/http/ngx_http_core_module.html#sendfile)、[腾讯云 CDN 新手指引](https://cloud.tencent.cn/document/product/228/43827)、[腾讯云 CDN 计费常见问题](https://cloud.tencent.com/document/product/228/43799)。
